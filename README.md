# Intern-task
This was the task assigned by Roxiler systems for their hiring.

In this task there were two parts - Frotend and backend
I completed the backend part.

DESCRIPTION
1.) Started with importing the required libraries such as express, mongoose and axios.
2.) Assigned port number
3.) Added middleware function express.json() to return results in json format
4.) Using mongoose, connected the database and created a connection
5.) Created the transaction Schema with all the given fields in 3rd party API url
6.) Initialized the database with the given 3rd party API url
7.) Started with the listing of Transactions API where Defined the query criteria and applied pagination.
8.) Next created the statistics part where did querying for the selected month
9.) Then created Bar chart and Pie chart using the given data after query completion
10.) At last, combined all the API's into one get API where all the results are fetched and combined
11.) Created PORT listen
